import { IsPageLayoutDirective } from './is-page-layout.directive';

describe('IsPageLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new IsPageLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
