import { OpenMenuOnMouseoverDirective } from './open-menu-on-mouseover.directive';

describe('OpenOnMouseoverDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenMenuOnMouseoverDirective();
    expect(directive).toBeTruthy();
  });
});
